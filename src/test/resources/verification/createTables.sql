CREATE TABLE if not exists intsedent (id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    shortinfo      TEXT              NOT NULL,
    info           TEXT              NOT NULL,
    extrainfo      TEXT);